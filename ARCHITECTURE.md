# UPSC AI Application - Architecture & Component Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        USER BROWSER                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    VITE DEV SERVER                          │   │
│  │                   (Localhost:3001)                          │   │
│  │                                                             │   │
│  │  ┌──────────┐  ┌──────────────┐  ┌─────────────────────┐  │   │
│  │  │index.html│─→│index.jsx     │─→│ App.jsx (Router)    │  │   │
│  │  └──────────┘  └──────────────┘  └────────┬────────────┘  │   │
│  │                                            │               │   │
│  │  ┌────────────────────────────────────────┼───────────┐   │   │
│  │  │                                        │           │   │   │
│  │  ▼                                        ▼           ▼   │   │
│  │ Layout.jsx         Login.jsx          Pages/        AI Assistant
│  │ (Sidebar)          (Auth)            • Dashboard.jsx    (Chat)
│  │                                      • NewsScraper.jsx  │   │
│  │                                      • SavedNotes.jsx   │   │
│  │                                                        │   │
│  │  Components/         Services/                         │   │
│  │  • ArticleCard.jsx   • authService.js──────────┐      │   │
│  │  • NoteViewer.jsx    • dbService.js ─┐         │      │   │
│  │                      • geminiService.js         │      │   │
│  └────────────────────────────────────────┼───────┼──────┘   │
│                                           │       │          │
└───────────────────────────────────────────┼───────┼──────────┘
                                            │       │
              ┌─────────────────────────────┘       │
              │                                     │
              ▼                                     ▼
    ┌──────────────────────────┐      ┌──────────────────────────┐
    │  BACKEND SERVER          │      │   GOOGLE GEMINI AI       │
    │  (Node.js/Express)       │      │   (Remote API)           │
    │  (Localhost:5000)        │      └──────────────────────────┘
    │                          │
    │  ┌────────────────────┐  │
    │  │  Routes/Endpoints  │  │
    │  │  • /api/auth/*     │  │
    │  │  • /api/notes      │  │
    │  │  • /api/articles   │  │
    │  │  • /api/health     │  │
    │  └────────────────────┘  │
    │           ▼              │
    │  ┌────────────────────┐  │
    │  │  Database Layer    │  │
    │  └────────────────────┘  │
    │           │              │
    └───────────┼──────────────┘
                │
    ┌───────────┴──────────┐
    │                      │
    ▼                      ▼
┌──────────────┐    ┌────────────────┐
│   MongoDB    │    │  In-Memory     │
│  (Cloud or   │    │  Storage       │
│   Local)     │    │  (Fallback)    │
└──────────────┘    └────────────────┘
```

---

## 🔄 Component Interaction Flow

```
Browser
  │
  ├─→ index.html
  │     │
  │     ├─→ Loads index.jsx
  │     │     │
  │     │     └─→ Creates React Root
  │     │           │
  │     │           └─→ Renders <App />
  │
  └─→ App.jsx (Router)
        │
        ├─→ Checks authentication status
        │     │
        │     └─→ authService.getStoredAuth()
        │           │
        │           └─→ Reads token from localStorage
        │
        ├─→ If NOT authenticated
        │     │
        │     └─→ Show <Login /> component
        │
        └─→ If authenticated
              │
              ├─→ Show <Layout /> component
              │     │
              │     ├─→ Sidebar navigation
              │     │
              │     └─→ Main content area
              │           │
              │           ├─→ <Dashboard /> page
              │           ├─→ <NewsScraper /> page
              │           ├─→ <SavedNotes /> page
              │
              └─→ All pages + <AIAssistant />
```

---

## 📊 Data Flow Diagram

### **Scenario 1: User Logs In**

```
┌──────────────────────────────────────────────────────────────────┐
│ User enters email/password and clicks "Sign In"                  │
└─────────────────────────┬──────────────────────────────────────┘
                          │
                          ▼
                  ┌──────────────────────┐
                  │  Login.jsx Component │
                  │  onSubmit handler    │
                  └──────────┬───────────┘
                             │
                             ▼
        ┌────────────────────────────────────────┐
        │ authService.login(email, password)     │
        │ [services/authService.js]              │
        └────────────┬─────────────────────────┘
                     │
                     ▼ (HTTP POST)
        ┌────────────────────────────────────────┐
        │ Backend: POST /api/auth/login          │
        │ http://localhost:5000                  │
        └────────────┬─────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────────┐
        │ server.js - Login Route Handler        │
        │                                        │
        │ 1. Find user in database               │
        │ 2. Compare password (bcrypt)           │
        │ 3. Generate JWT token                  │
        │ 4. Return { token, user }              │
        └────────────┬─────────────────────────┘
                     │
        ┌────────────┴──────────────────────────┐
        │ Response includes JWT token            │
        │ Example:                               │
        │ {                                      │
        │   token: "eyJhbGciOiJIUzI1...",       │
        │   user: { name: "Ayush", ... }       │
        │ }                                      │
        └────────────┬──────────────────────────┘
                     │ (HTTP 200 OK)
                     ▼
        ┌────────────────────────────────────────┐
        │ Frontend receives response              │
        │ [authService.js]                       │
        └────────────┬─────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────────┐
        │ Save to localStorage:                  │
        │                                        │
        │ localStorage.setItem(                  │
        │   'upsc_auth_token',                   │
        │   'eyJhbGciOiJIUzI1...'                │
        │ )                                      │
        │                                        │
        │ localStorage.setItem(                  │
        │   'upsc_auth_user',                    │
        │   JSON.stringify({ name, email, ... })│
        │ )                                      │
        └────────────┬─────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────────────────┐
        │ Navigate to "/" (Dashboard)            │
        │ [useNavigate() hook]                   │
        └──────────────────────────────────────┘
```

---

### **Scenario 2: User Scrapes News**

```
┌────────────────────────────────────────────────────────────┐
│ User visits NewsScraper page                               │
│ Selects: BBC, Topic: Economy                               │
│ Clicks "Scrape News" button                                │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────────┐
        │ NewsScraper.jsx - handleScrape()     │
        └──────────────────┬───────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ geminiService.fetchNewsViaAI("BBC", "Economy")   │
        │ [services/geminiService.js]                      │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼ (HTTPS to Google API)
        ┌──────────────────────────────────────────────────┐
        │ Google Gemini 2.5 Flash Model                    │
        │                                                  │
        │ Prompt: "Find latest economy news from BBC"      │
        │                                                  │
        │ Uses: Google Search + AI                         │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼ (Response with news)
        ┌──────────────────────────────────────────────────┐
        │ Gemini returns JSON:                             │
        │ [                                                │
        │   {                                              │
        │     title: "UK Economy Grows...",               │
        │     summary: "The UK economy...",               │
        │     url: "https://bbc.com/...",                 │
        │     date: "2026-01-21"                          │
        │   },                                             │
        │   ... (4 more articles)                          │
        │ ]                                                │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ Frontend displays articles on screen             │
        │ User sees 5 news articles from BBC               │
        └──────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ User clicks "Generate Notes" on one article      │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ geminiService.generateNoteFromContent()          │
        │ Sends article text to Gemini                     │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ Gemini analyzes for UPSC:                        │
        │ - GS Paper: GS3 (Economy)                        │
        │ - MCQs: 3 high-quality questions                 │
        │ - Mains Q&A: Model answer structure              │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ Frontend displays structured notes               │
        │ User clicks "Save Note"                          │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ dbService.saveNote(noteData)                     │
        │ Sends POST to backend                            │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ Backend: POST /api/notes                         │
        │ - Receives note data                             │
        │ - Verifies JWT token                             │
        │ - Gets userId from token                         │
        │ - Saves note to MongoDB                          │
        │ - Returns saved note                             │
        └──────────────────┬───────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────────────────┐
        │ Frontend receives success response               │
        │ Shows "Note saved!" message                      │
        │ Note appears in SavedNotes page                  │
        └──────────────────────────────────────────────────┘
```

---

## 📱 Page-by-Page Layout

### **Login Page**
```
┌─────────────────────────────────────┐
│         UPSC AI Login               │
├─────────────────────────────────────┤
│                                     │
│  Email:     [________________]      │
│                                     │
│  Password:  [________________]      │
│                                     │
│  [Sign In]  [Sign Up]              │
│                                     │
└─────────────────────────────────────┘
```

### **Dashboard Page**
```
┌──────────────────────────────────────────────────────┐
│  ☰  UPSC AI   │                                      │
├───────────────┼──────────────────────────────────────┤
│ Dashboard     │   Welcome, Ayush!                    │
│ News Scraper  │                                      │
│ Saved Notes   │   Recent Articles                    │
│ Settings      │   ┌──────────────────────────────┐  │
│ Logout        │   │ Article Title                │  │
│               │   │ Source: BBC | Date: 1/21    │  │
│               │   │ [Generate Notes]             │  │
│               │   └──────────────────────────────┘  │
│               │                                      │
│               │   Saved Notes                        │
│               │   ┌──────────────────────────────┐  │
│               │   │ Note: GS Paper 3 Economy    │  │
│               │   │ Tags: #economy #india       │  │
│               │   │ [View] [Download] [Delete]  │  │
│               │   └──────────────────────────────┘  │
│               │                                      │
└───────────────┴──────────────────────────────────────┘
    [Floating Chat Button 💬]
```

### **News Scraper Page**
```
┌──────────────────────────────────────────────────────┐
│  ☰  UPSC AI   │                                      │
├───────────────┼──────────────────────────────────────┤
│ Dashboard     │   News Scraper                       │
│ News Scraper  │                                      │
│ Saved Notes   │   Source: [BBC ▼]                    │
│ Settings      │   Topic:  [Economy ▼]                │
│ Logout        │                                      │
│               │   [🔍 Scrape News]                   │
│               │                                      │
│               │   Found 5 articles                   │
│               │   ┌──────────────────────────────┐  │
│               │   │ 1. Big Capex Challenge       │  │
│               │   │    With roads and railways.. │  │
│               │   │ [Generate Notes] [Save]     │  │
│               │   └──────────────────────────────┘  │
│               │                                      │
│               │   ┌──────────────────────────────┐  │
│               │   │ 2. MMRDA Signs 10 MOUs       │  │
│               │   │    Mumbai development auth.. │  │
│               │   │ [Generate Notes] [Save]     │  │
│               │   └──────────────────────────────┘  │
│               │                                      │
└───────────────┴──────────────────────────────────────┘
```

---

## 🔐 Authentication Lifecycle

```
APP STARTS
    │
    ├─→ Check localStorage for token
    │     │
    │     ├─→ Token exists?
    │     │     │
    │     │     ├─→ YES → Skip login, show Dashboard
    │     │     │
    │     │     └─→ NO → Show Login page
    │
    └─→ User Login Process
          │
          ├─→ Enter credentials (email, password)
          │
          ├─→ Send to backend
          │
          ├─→ Backend validates
          │
          ├─→ Backend generates JWT token
          │
          ├─→ Frontend saves token to localStorage
          │
          ├─→ All future API calls include token
          │
          ├─→ Backend verifies token for each request
          │
          └─→ User logged in ✓

USER NAVIGATES
    │
    ├─→ Click "Dashboard"
    │     │
    │     ├─→ Dashboard.jsx loads
    │     │
    │     ├─→ Calls db.getNotes()
    │     │
    │     ├─→ dbService includes token in header:
    │     │    Authorization: "Bearer {token}"
    │     │
    │     ├─→ Backend receives request
    │     │
    │     ├─→ Backend verifies token is valid
    │     │
    │     ├─→ Returns user's notes
    │     │
    │     └─→ Display notes on screen

USER LOGS OUT
    │
    ├─→ Click "Logout"
    │
    ├─→ authService.logout() called
    │
    ├─→ Remove token from localStorage
    │
    ├─→ Remove user data from localStorage
    │
    ├─→ Redirect to /login
    │
    └─→ User logged out ✓
```

---

## 🚀 Startup Sequence

```
1. USER OPENS BROWSER
   └─→ http://localhost:3001

2. VITE DEV SERVER
   └─→ Serves index.html

3. BROWSER LOADS index.html
   ├─→ Loads CSS (Tailwind)
   ├─→ Creates <div id="root">
   └─→ Executes <script src="/index.jsx">

4. index.jsx RUNS
   ├─→ Imports React & App component
   ├─→ Creates React root
   └─→ Renders <App />

5. App.jsx RENDERS
   ├─→ Sets up router (HashRouter)
   ├─→ Checks: is user authenticated?
   ├─→ If NO → Show Login page
   └─→ If YES → Show Dashboard with Layout

6. BACKEND SERVER RUNNING
   ├─→ Listens on localhost:5000
   ├─→ Database connected (MongoDB or fallback)
   └─→ Ready to handle API requests

7. USER SEES FULLY LOADED APP
   ├─→ Can login/register
   ├─→ Can browse pages
   ├─→ Can scrape news
   ├─→ Can chat with AI
   └─→ Everything working ✓
```

---

## 📚 Technology Stack Summary

```
Frontend:
├─ React 19.2.1          (UI components)
├─ React Router 7.10.1   (Page routing)
├─ Vite 6.4.1            (Build tool)
├─ Tailwind CSS          (Styling)
├─ Lucide React          (Icons)
└─ React Markdown        (Markdown rendering)

Backend:
├─ Express.js            (HTTP server)
├─ MongoDB/Mongoose      (Database)
├─ JWT                   (Authentication)
├─ bcryptjs              (Password hashing)
└─ CORS                  (Cross-origin support)

External APIs:
├─ Google Gemini 2.5     (AI/NLP)
│  └─ News scraping
│  └─ Text analysis
│  └─ UPSC study material generation
│
└─ Google Fonts          (Typography)

Storage:
├─ MongoDB               (Production)
└─ localStorage/RAM      (Fallback)
```

